[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "water-energy-coopt"
version = "1.0.1"
description = "Household water-supply technology MILP behind Jones et al. (2021), Sustainable Cities and Society"
readme = "README.md"
requires-python = ">=3.10,<3.14"
license = {file = "LICENSE"}
authors = [{name = "Erick C. Jones, Jr.", email = "erick.jones@uta.edu"}]
dependencies = [
    "gurobipy>=11,<14",
    "PyYAML>=6,<7",
    "matplotlib>=3.7,<4",
]

[project.optional-dependencies]
dev = ["pytest>=7,<9"]
# Executing the notebooks, which is how they are shipped with outputs.
notebooks = ["jupyter>=1,<2", "nbformat>=5,<6", "nbclient>=0.9,<1"]

[tool.setuptools.packages.find]
where = ["src"]

# The config and scenarios live INSIDE the package so they ship in the wheel.
# Without this a `pip install` yields a package that cannot find its own data.
[tool.setuptools.package-data]
water_energy = ["config.yaml", "scenarios/*.yaml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
